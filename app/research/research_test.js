'use strict';

describe('myApp.research module', function() {

  beforeEach(module('myApp.research'));

  describe('research controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var researchCtrl = $controller('ResearchCtrl');
      expect(researchCtrl).toBeDefined();
    }));

  });
});